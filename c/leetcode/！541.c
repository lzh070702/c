#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* reverseStr(char* s, int k) {
    
    return s;
}

int main() {
    char s[] = "abcdefg";
    int k = 8;
    printf("%s\n", reverseStr(s, k));
    return 0;
}